namespace TypingMe.Data
{
    /// <summary>Difficulty band a word belongs to. See <see cref="WordBankSO"/> for the tiering rules.</summary>
    public enum WordTier
    {
        Easy = 0,
        Medium = 1,
        Hard = 2
    }
}
